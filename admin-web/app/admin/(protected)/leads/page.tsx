import{cookies}from"next/headers";import{LeadsDashboard}from"@/components/leads-dashboard";import{fetchLeads}from"@/lib/leads-api";import{ADMIN_SESSION_COOKIE}from"@/lib/session";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const params=await searchParams;const filters={page:String(params.page||"1"),page_size:"20",search:String(params.search||""),status:String(params.status||"all")};const token=(await cookies()).get(ADMIN_SESSION_COOKIE)?.value||"";const data=await fetchLeads(new URLSearchParams(filters),token);return data?<LeadsDashboard data={data} filters={filters}/>:<section className="state-panel error-state"><strong>Leads could not be loaded.</strong></section>}
