import{cookies}from"next/headers";import{ResultsDashboard}from"@/components/publication-dashboards";import{fetchResults}from"@/lib/publications-api";import{ADMIN_SESSION_COOKIE}from"@/lib/session";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;const f={page:String(p.page||"1"),page_size:"20",search:String(p.search||""),verification:String(p.verification||"all"),publication:String(p.publication||"all"),symbol:String(p.symbol||""),outcome:String(p.outcome||"all")};const data=await fetchResults(new URLSearchParams(f),(await cookies()).get(ADMIN_SESSION_COOKIE)?.value||"");return data?<ResultsDashboard data={data} filters={f}/>:<section className="state-panel error-state"><strong>Verified results could not be loaded.</strong></section>}
