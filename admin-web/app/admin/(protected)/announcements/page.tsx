import{cookies}from"next/headers";import{AnnouncementsDashboard}from"@/components/publication-dashboards";import{fetchAnnouncements}from"@/lib/publications-api";import{ADMIN_SESSION_COOKIE}from"@/lib/session";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;const f={page:String(p.page||"1"),page_size:"20",search:String(p.search||""),status:String(p.status||"all"),kind:String(p.kind||"all"),priority:String(p.priority||"all"),audience:String(p.audience||"all")};const token=(await cookies()).get(ADMIN_SESSION_COOKIE)?.value||"";const data=await fetchAnnouncements(new URLSearchParams(f),token);return data?<AnnouncementsDashboard data={data} filters={f}/>:<section className="state-panel error-state"><strong>Announcements could not be loaded.</strong></section>}
