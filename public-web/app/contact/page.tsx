import type { Metadata } from "next";
import { configuredLinks } from "@/lib/site-config";

export const metadata: Metadata = { title: "Contact", description: "Configured public contact channels for VenusRealm." };
export default function ContactPage() { const links = configuredLinks(); const channels = [["Telegram",links.telegram],["WhatsApp",links.whatsapp],["YouTube",links.youtube]].filter((entry): entry is [string,string] => Boolean(entry[1])); return <article className="content-page"><header className="content-page-header"><span className="eyebrow">CONTACT</span><h1>Reach VenusRealm through a verified channel.</h1><p>Only channels configured by the site operator are displayed. VenusRealm will never request passwords, recovery phrases or private keys.</p></header>{channels.length ? <div className="offer-grid">{channels.map(([label, href]) => <a className="offer-card" href={href} rel="noreferrer" target="_blank" key={label}><h3>{label}</h3><p>Open the configured public VenusRealm {label} destination.</p><span className="text-link">Open channel →</span></a>)}</div> : <div className="empty-state">No public contact channel is configured for this preview.</div>}</article>; }
