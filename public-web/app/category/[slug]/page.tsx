import { ContentGrid } from "@/components/content-grid";
import { getCategories, getContent } from "@/lib/api";
export default async function CategoryPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const [categories, content] = await Promise.all([getCategories(), getContent(undefined, 80)]); const category = categories.find((item) => item.slug === slug); const items = content.filter((item) => item.category_slug === slug); return <section><div className="page-heading"><small>MARKET CATEGORY</small><h1>{category?.title || slug.replaceAll("-", " ")}</h1><p>{category?.description || "Published market content and analysis."}</p></div><ContentGrid items={items} /></section>; }
